# LeetCode- Simplify Path

## Question

Given a path of a Linux/Unix file system, simplify it and return the final valid path.

`.` means current directory.

`..` means go to the previous directory.

Multiple `/` should be treated as a single `/`.