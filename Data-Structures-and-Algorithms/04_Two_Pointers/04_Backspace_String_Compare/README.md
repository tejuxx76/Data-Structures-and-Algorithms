# LeetCodeBackspace String Compare
## Question
Given two strings, compare them after processing `#` as a backspace.
Return `True` if both strings are equal after processing.
Otherwise, return `False`.